Right now we assume the adress of the switches are at 0.0.0.0

The thirft and grpc port passed to the simple_switch_grpc are for the host


The timestamps in the bmv2 models are in microseconds.

Commands for the controller :
./controller.py --output logs/s1.controller.csv --grpc-port 50051 --thrift-port 9090 --device-id 0 --classe 19
./controller.py --output logs/s2.controller.csv --grpc-port 50052 --thrift-port 9091 --device-id 1 --classe 22
./controller.py --output logs/s3.controller.csv --grpc-port 50053 --thrift-port 9092 --device-id 2 --classe 25


grep -E ,1,[0-9]+$ unsw_cl_2_0_results_DUNE.csv


DEBUG :

It looks like that most packets that are missing from the bmv2
log are with pkt_count == 0, that is just after a register reset.
Maybe there is something wrong with my controller sleeping

To explain bad results on second switch, flows that should have been classified on
the first switch but were not because of the time related features then appear in
the second one and so on.
